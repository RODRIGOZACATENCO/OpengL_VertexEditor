#include <gtest/gtest.h>
#include "CameraHandler.h"

TEST(CameraTest, Initialization) {
    // CameraHandler is currently mostly a stub, so just test it compiles
    CameraHandler camera;
    EXPECT_TRUE(true);
}
